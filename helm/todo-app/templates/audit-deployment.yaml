{{- if .Values.auditService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-app.fullname" . }}-audit
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "todo-app.auditLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.auditService.replicaCount }}
  selector:
    matchLabels:
      {{- include "todo-app.auditSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "todo-app.auditSelectorLabels" . | nindent 8 }}
      annotations:
        {{- if .Values.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: "audit-service"
        dapr.io/app-port: "{{ .Values.auditService.service.targetPort }}"
        dapr.io/config: {{ .Values.dapr.config | quote }}
        dapr.io/app-protocol: "http"
        {{- end }}
    spec:
      {{- if .Values.serviceAccount.create }}
      serviceAccountName: {{ include "todo-app.fullname" . }}-service
      {{- end }}
      containers:
      - name: audit
        image: {{ include "todo-app.auditImage" . }}
        imagePullPolicy: {{ .Values.auditService.image.pullPolicy | default .Values.global.imagePullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.auditService.service.targetPort }}
          protocol: TCP
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: DATABASE_URL
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: {{ include "todo-app.fullname" . }}-config
              key: KAFKA_BROKERS
        - name: DAPR_HTTP_PORT
          value: "{{ .Values.dapr.httpPort }}"
        - name: DAPR_GRPC_PORT
          value: "{{ .Values.dapr.grpcPort }}"
        - name: TASK_EVENTS_TOPIC
          value: {{ .Values.kafka.topics.taskEvents | quote }}
        - name: AUDIT_RETENTION_DAYS
          value: {{ .Values.auditService.auditRetentionDays | quote }}
        resources:
          {{- toYaml .Values.auditService.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: {{ .Values.auditService.livenessProbe.path }}
            port: {{ .Values.auditService.service.targetPort }}
          initialDelaySeconds: {{ .Values.auditService.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.auditService.livenessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.auditService.livenessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.auditService.livenessProbe.failureThreshold }}
        readinessProbe:
          httpGet:
            path: {{ .Values.auditService.readinessProbe.path }}
            port: {{ .Values.auditService.service.targetPort }}
          initialDelaySeconds: {{ .Values.auditService.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.auditService.readinessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.auditService.readinessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.auditService.readinessProbe.failureThreshold }}
{{- end }}
---
{{- if .Values.auditService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.fullname" . }}-audit
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "todo-app.auditLabels" . | nindent 4 }}
spec:
  type: {{ .Values.auditService.service.type }}
  selector:
    {{- include "todo-app.auditSelectorLabels" . | nindent 4 }}
  ports:
  - name: http
    port: {{ .Values.auditService.service.port }}
    targetPort: http
    protocol: TCP
{{- end }}
